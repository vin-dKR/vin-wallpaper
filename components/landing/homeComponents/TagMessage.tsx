const TagMessges = () => {
    return (
        <div className="absolute inset-0 flex flex-col items-center justify-center -top-[15em] md:-top-[20em] bg-blur-lg">
            <span className="relative text-[8px] sm:text-sm text-white bg-white/20 py-1 sm:pt-2 px-2 sm:px-4 rounded-xl border border-white/30">
                an AI Wallpaper Bot for 𝕏
            </span>
        </div>
    );
};

export default TagMessges
